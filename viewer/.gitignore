# Build artifacts — regenerated by `make viewer`
data.json

# The generated viewer is written to the spec directory (parent), not here,
# but ignore it too if it ever lands in this folder.
capability-model-viewer.html
